; counters.cpu
;
; Increments registers until they overflow, then add 1 to the next
; register and continue, starting with A, then X, then Y.
;
:START
INA         ; Increment A
JNO START   ; If A did not overflow, go back to the start
INX         ; A did overflow, increment X
JNO START   ; If X did not overflow, go back to the start
INY         ; X did overflow, increment Y
JNO START   ; If Y did not overflow, go back to the start
HLT         ; Y did overflow, halt the program